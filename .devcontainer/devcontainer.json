{
  "name": "skills-vue-modular",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "mounts": [
    "source=${localEnv:USERPROFILE}/.ssh,target=/home/dev/.ssh,type=bind,consistency=cached"
  ]
}